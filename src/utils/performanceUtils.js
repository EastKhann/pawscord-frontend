/**
 * @file performanceUtils.js — Legacy re-export
 * All implementations now live in ./performanceHooks.js
 */
export {
    deepEqual,
    memoizeDeep,
    debounce,
    throttle,
    useDebouncedState,
    useThrottledCallback,
    usePrevious,
    useIntersectionObserver,
    useRenderCount,
    useRenderTime,
    useCleanup,
    performanceMetrics,
    rafScheduler,
    smoothScrollTo,
    getVisibleItems,
    CleanupManager,
} from './performanceHooks';

import {
    deepEqual,
    memoizeDeep,
    debounce,
    throttle,
    useDebouncedState,
    useThrottledCallback,
    usePrevious,
    useIntersectionObserver,
    useRenderCount,
    useRenderTime,
    useCleanup,
    performanceMetrics,
    rafScheduler,
    smoothScrollTo,
    getVisibleItems,
    CleanupManager,
} from './performanceHooks';

export default {
    deepEqual,
    memoizeDeep,
    debounce,
    throttle,
    useDebouncedState,
    useThrottledCallback,
    usePrevious,
    useIntersectionObserver,
    useRenderCount,
    useRenderTime,
    useCleanup,
    performanceMetrics,
    rafScheduler,
    smoothScrollTo,
    getVisibleItems,
    CleanupManager,
};
