/**
 * @file renderOptimization.js — Legacy re-export
 * All implementations now live in ./performanceHooks.js
 */
export {
    deepMemo,
    OptimizedMessage,
    useStableFunctions,
    useVirtualList,
    useDebouncedState,
    useThrottledCallback,
    useIntersectionObserver,
    shallowEqual,
    useBatchedState,
    useSelector,
} from './performanceHooks';

import {
    deepMemo,
    OptimizedMessage,
    useStableFunctions,
    useVirtualList,
    useDebouncedState,
    useThrottledCallback,
    useIntersectionObserver,
    shallowEqual,
    useBatchedState,
    useSelector,
} from './performanceHooks';

export default {
    deepMemo,
    OptimizedMessage,
    useStableFunctions,
    useVirtualList,
    useDebouncedState,
    useThrottledCallback,
    useIntersectionObserver,
    shallowEqual,
    useBatchedState,
    useSelector,
};



