/**
 * OpenAPI schema type stub.
 *
 * This file is a minimal stub so the codebase compiles without a live server.
 * To regenerate from the real OpenAPI spec, run:
 *   npm run api:generate
 *
 * The stub exports the same shape expected by openapi-fetch's createClient<paths>.
 */

export type paths = Record<string, unknown>;
export type components = {
    schemas: Record<string, unknown>;
};
export type operations = Record<string, unknown>;
